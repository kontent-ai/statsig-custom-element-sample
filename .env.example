# Statsig Console API Key (get from Statsig console: Settings → Keys & Environments → Console API)
STATSIG_CONSOLE_KEY=console-xxx

# -----------Kontent.ai API Keys-----------------------
# Required for cleanup/conclude experiment functionality and sync content type script
# Get from Kontent.ai: Environment settings → API keys

# Management API key - for modifying content items during cleanup
KONTENT_MANAGEMENT_API_KEY=your-management-api-key

# Preview Delivery API key - for finding items that reference the experiment (used-in endpoint)
KONTENT_PREVIEW_API_KEY=your-preview-api-key

# -----------Only needed for sync content type script-----------------------
# Kontent.ai environment ID
KONTENT_ENVIRONMENT_ID=your-environment-id

# URL where the custom element is deployed (must be HTTPS)
CUSTOM_ELEMENT_URL=https://your-deployed-element.netlify.app

# Optional: custom codename for the experiment content type (default: statsig_experiment)
# CONTENT_TYPE_CODENAME=statsig_experiment
